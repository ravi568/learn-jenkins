node('workstation') {
      try{
        stage("one") {
              echo "one"
           }

        stage("two") {
               echo "two"
        }
     } catch(e) {
         echo 'this will only run if failed'
     }

}